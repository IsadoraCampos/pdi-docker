import { Button } from '../button/Button';

export const App = () => {
    return (
        <div>
            <h1 className="title">Welcome!</h1>
            <Button description='Click me!' />
        </div>
    );
}
